import { e as createComponent, k as renderComponent, r as renderTemplate, l as Fragment, h as addAttribute, u as unescapeHTML, m as maybeRenderHead } from '../chunks/astro/server_Cpt8RX-b.mjs';
import { $ as $$Layout } from '../chunks/Layout_CTgb0INN.mjs';
/* empty css                                       */
/* empty css                                 */
export { renderers } from '../renderers.mjs';

var __freeze = Object.freeze;
var __defProp = Object.defineProperty;
var __template = (cooked, raw) => __freeze(__defProp(cooked, "raw", { value: __freeze(raw || cooked.slice()) }));
var _a;
const $$Index = createComponent(async ($$result, $$props, $$slots) => {
  let noticias = [];
  function formatDate(dateString) {
    const date = new Date(dateString);
    return new Intl.DateTimeFormat("es-ES", {
      year: "numeric",
      month: "long",
      day: "numeric"
    }).format(date);
  }
  return renderTemplate`${renderComponent($$result, "Layout", $$Layout, { "title": "Noticias del Real Madrid Femenino | Madrid Femenino Xtra", "description": "\xDAltima hora del Real Madrid femenino en directo: actualidad, pr\xF3ximos partidos y fichajes en Liga F, UWCL, Copa de la Reina y Supercopa de Espa\xF1a." }, { "default": async ($$result2) => renderTemplate(_a || (_a = __template([" ", '<main class="noticias-page"> <div class="container"> <header> <h1 class="page-title">NOTICIAS</h1> <h2 class="content-h2">\n\xDAltima hora del Real Madrid femenino en directo: actualidad,\n                    pr\xF3ximos partidos y fichajes en Liga F, UWCL, Copa de la\n                    Reina y Supercopa de Espa\xF1a.\n</h2> </header>  ', "  ", "  ", ' </div>\n) }\n</main> <script>\n        const WP_URL =\n            "https://cms.madridfemeninoxtra.com/wp-json/wp/v2/posts?per_page=50&_embed=true";\n\n        function formatDate(dateString) {\n            const date = new Date(dateString);\n            return new Intl.DateTimeFormat("es-ES", {\n                year: "numeric",\n                month: "long",\n                day: "numeric",\n            }).format(date);\n        }\n\n        function createNewsCard(noticia) {\n            const featuredImage =\n                noticia._embedded?.["wp:featuredmedia"]?.[0]?.source_url;\n            const featuredImageAlt =\n                noticia._embedded?.["wp:featuredmedia"]?.[0]?.alt_text ||\n                noticia.title.rendered;\n            const categories =\n                noticia._embedded?.["wp:term"]?.[0]?.map((cat) => cat.name) ||\n                [];\n\n            const link = `/noticias/${noticia.slug}`;\n\n            return `\n                <article class="noticia-card" onclick="window.location.href=\'${link}\'" style="cursor: pointer;">\n                    ${\n                        featuredImage\n                            ? `\n                        <div class="noticia-image">\n                            <img src="${featuredImage}" alt="${featuredImageAlt}" loading="lazy" />\n                        </div>\n                    `\n                            : ""\n                    }\n                    <div class="noticia-content">\n                        <div class="noticia-meta">\n                            <time datetime="${noticia.date}">${formatDate(noticia.date)}</time>\n                            ${categories.length > 0 ? `<span class="category">${categories[0]}</span>` : ""}\n                        </div>\n                        <h2 class="noticia-title">${noticia.title.rendered}</h2>\n                        <div class="noticia-excerpt">${noticia.excerpt.rendered}</div>\n                        <a href="${link}" class="read-more">\n                            Leer m\xE1s\n                            <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">\n                                <path d="M5 12h14M12 5l7 7-7 7" />\n                            </svg>\n                        </a>\n                    </div>\n                </article>\n            `;\n        }\n\n        async function loadNews() {\n            try {\n                const response = await fetch(WP_URL);\n                if (!response.ok) throw new Error("Failed to fetch");\n\n                const posts = await response.json();\n\n                // Find all sections and grids\n                const sections = [\n                    {\n                        filter: (cat) => cat.toLowerCase().includes("previa"),\n                        containerIndex: 0,\n                    },\n                    {\n                        filter: (cat) => cat.toLowerCase() === "liga f",\n                        containerIndex: 1,\n                    },\n                    {\n                        filter: (cat) => cat.toLowerCase() === "uwcl",\n                        containerIndex: 2,\n                    },\n                    {\n                        filter: (cat) =>\n                            cat.toLowerCase() === "uncategorized" ||\n                            cat.toLowerCase() === "sin categor\xEDa",\n                        containerIndex: 3,\n                    },\n                ];\n\n                const grids = document.querySelectorAll(".noticias-grid");\n                const noNewsMessages =\n                    document.querySelectorAll(".no-category-news");\n\n                sections.forEach((section, idx) => {\n                    const filtered = posts.filter((p) =>\n                        p._embedded?.["wp:term"]?.[0]?.some((cat) =>\n                            section.filter(cat.name),\n                        ),\n                    );\n\n                    const grid = grids[idx];\n                    const noNews = noNewsMessages[idx];\n\n                    if (filtered.length > 0 && grid) {\n                        grid.innerHTML = filtered.map(createNewsCard).join("");\n                        if (noNews) noNews.style.display = "none";\n                    } else if (grid) {\n                        grid.style.display = "none";\n                    }\n                });\n            } catch (error) {\n                console.error("Error loading news:", error);\n            }\n        }\n\n        // Load news when page is ready\n        if (document.readyState === "loading") {\n            document.addEventListener("DOMContentLoaded", loadNews);\n        } else {\n            loadNews();\n        }\n    <\/script> '], [" ", '<main class="noticias-page"> <div class="container"> <header> <h1 class="page-title">NOTICIAS</h1> <h2 class="content-h2">\n\xDAltima hora del Real Madrid femenino en directo: actualidad,\n                    pr\xF3ximos partidos y fichajes en Liga F, UWCL, Copa de la\n                    Reina y Supercopa de Espa\xF1a.\n</h2> </header>  ', "  ", "  ", ' </div>\n) }\n</main> <script>\n        const WP_URL =\n            "https://cms.madridfemeninoxtra.com/wp-json/wp/v2/posts?per_page=50&_embed=true";\n\n        function formatDate(dateString) {\n            const date = new Date(dateString);\n            return new Intl.DateTimeFormat("es-ES", {\n                year: "numeric",\n                month: "long",\n                day: "numeric",\n            }).format(date);\n        }\n\n        function createNewsCard(noticia) {\n            const featuredImage =\n                noticia._embedded?.["wp:featuredmedia"]?.[0]?.source_url;\n            const featuredImageAlt =\n                noticia._embedded?.["wp:featuredmedia"]?.[0]?.alt_text ||\n                noticia.title.rendered;\n            const categories =\n                noticia._embedded?.["wp:term"]?.[0]?.map((cat) => cat.name) ||\n                [];\n\n            const link = \\`/noticias/\\${noticia.slug}\\`;\n\n            return \\`\n                <article class="noticia-card" onclick="window.location.href=\'\\${link}\'" style="cursor: pointer;">\n                    \\${\n                        featuredImage\n                            ? \\`\n                        <div class="noticia-image">\n                            <img src="\\${featuredImage}" alt="\\${featuredImageAlt}" loading="lazy" />\n                        </div>\n                    \\`\n                            : ""\n                    }\n                    <div class="noticia-content">\n                        <div class="noticia-meta">\n                            <time datetime="\\${noticia.date}">\\${formatDate(noticia.date)}</time>\n                            \\${categories.length > 0 ? \\`<span class="category">\\${categories[0]}</span>\\` : ""}\n                        </div>\n                        <h2 class="noticia-title">\\${noticia.title.rendered}</h2>\n                        <div class="noticia-excerpt">\\${noticia.excerpt.rendered}</div>\n                        <a href="\\${link}" class="read-more">\n                            Leer m\xE1s\n                            <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">\n                                <path d="M5 12h14M12 5l7 7-7 7" />\n                            </svg>\n                        </a>\n                    </div>\n                </article>\n            \\`;\n        }\n\n        async function loadNews() {\n            try {\n                const response = await fetch(WP_URL);\n                if (!response.ok) throw new Error("Failed to fetch");\n\n                const posts = await response.json();\n\n                // Find all sections and grids\n                const sections = [\n                    {\n                        filter: (cat) => cat.toLowerCase().includes("previa"),\n                        containerIndex: 0,\n                    },\n                    {\n                        filter: (cat) => cat.toLowerCase() === "liga f",\n                        containerIndex: 1,\n                    },\n                    {\n                        filter: (cat) => cat.toLowerCase() === "uwcl",\n                        containerIndex: 2,\n                    },\n                    {\n                        filter: (cat) =>\n                            cat.toLowerCase() === "uncategorized" ||\n                            cat.toLowerCase() === "sin categor\xEDa",\n                        containerIndex: 3,\n                    },\n                ];\n\n                const grids = document.querySelectorAll(".noticias-grid");\n                const noNewsMessages =\n                    document.querySelectorAll(".no-category-news");\n\n                sections.forEach((section, idx) => {\n                    const filtered = posts.filter((p) =>\n                        p._embedded?.["wp:term"]?.[0]?.some((cat) =>\n                            section.filter(cat.name),\n                        ),\n                    );\n\n                    const grid = grids[idx];\n                    const noNews = noNewsMessages[idx];\n\n                    if (filtered.length > 0 && grid) {\n                        grid.innerHTML = filtered.map(createNewsCard).join("");\n                        if (noNews) noNews.style.display = "none";\n                    } else if (grid) {\n                        grid.style.display = "none";\n                    }\n                });\n            } catch (error) {\n                console.error("Error loading news:", error);\n            }\n        }\n\n        // Load news when page is ready\n        if (document.readyState === "loading") {\n            document.addEventListener("DOMContentLoaded", loadNews);\n        } else {\n            loadNews();\n        }\n    <\/script> '])), maybeRenderHead(), renderComponent($$result2, "Fragment", Fragment, {}, { "default": async ($$result3) => renderTemplate` <h2 class="section-title">PREVIAS</h2> ${noticias.filter(
    (n) => n.categories.some(
      (cat) => cat.toLowerCase().includes("previas")
    )
  ).length > 0 ? renderTemplate`<div class="noticias-grid"> ${noticias.filter(
    (n) => n.categories.some(
      (cat) => cat.toLowerCase().includes("previas")
    )
  ).map((noticia) => renderTemplate`<article class="noticia-card"> ${noticia.featuredImage && renderTemplate`<div class="noticia-image"> <img${addAttribute(noticia.featuredImage, "src")}${addAttribute(
    noticia.featuredImageAlt || noticia.title,
    "alt"
  )} loading="lazy"> </div>`} <div class="noticia-content"> <div class="noticia-meta"> <time${addAttribute(noticia.date, "datetime")}> ${formatDate(noticia.date)} </time> ${noticia.categories.length > 0 && renderTemplate`<span class="category"> ${noticia.categories[0]} </span>`} </div> <h2 class="noticia-title"> ${noticia.title} </h2> <div class="noticia-excerpt">${unescapeHTML(noticia.excerpt)}</div> <a${addAttribute(noticia.link, "href")} class="read-more">
Leer más
<svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"> <path d="M5 12h14M12 5l7 7-7 7"></path> </svg> </a> </div> </article>`)} </div>` : renderTemplate`<p class="no-category-news">
No hay previas disponibles.
</p>`}` }), renderComponent($$result2, "Fragment", Fragment, {}, { "default": async ($$result3) => renderTemplate` <h2 class="section-title">LIGA F</h2> ${noticias.filter(
    (n) => n.categories.some(
      (cat) => cat.toLowerCase() === "liga f"
    )
  ).length > 0 ? renderTemplate`<div class="noticias-grid"> ${noticias.filter(
    (n) => n.categories.some(
      (cat) => cat.toLowerCase() === "liga f"
    )
  ).map((noticia) => renderTemplate`<article class="noticia-card"> ${noticia.featuredImage && renderTemplate`<div class="noticia-image"> <img${addAttribute(noticia.featuredImage, "src")}${addAttribute(
    noticia.featuredImageAlt || noticia.title,
    "alt"
  )} loading="lazy"> </div>`} <div class="noticia-content"> <div class="noticia-meta"> <time${addAttribute(noticia.date, "datetime")}> ${formatDate(noticia.date)} </time> ${noticia.categories.length > 0 && renderTemplate`<span class="category"> ${noticia.categories[0]} </span>`} </div> <h2 class="noticia-title"> ${noticia.title} </h2> <div class="noticia-excerpt">${unescapeHTML(noticia.excerpt)}</div> <a${addAttribute(noticia.link, "href")} class="read-more">
Leer más
<svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"> <path d="M5 12h14M12 5l7 7-7 7"></path> </svg> </a> </div> </article>`)} </div>` : renderTemplate`<p class="no-category-news">
No hay noticias de Liga F disponibles.
</p>`}` }), renderComponent($$result2, "Fragment", Fragment, {}, { "default": async ($$result3) => renderTemplate` <h2 class="section-title">UWCL</h2> ${noticias.filter(
    (n) => n.categories.some(
      (cat) => cat.toLowerCase() === "uwcl"
    )
  ).length > 0 ? renderTemplate`<div class="noticias-grid"> ${noticias.filter(
    (n) => n.categories.some(
      (cat) => cat.toLowerCase() === "uwcl"
    )
  ).map((noticia) => renderTemplate`<article class="noticia-card"> ${noticia.featuredImage && renderTemplate`<div class="noticia-image"> <img${addAttribute(noticia.featuredImage, "src")}${addAttribute(
    noticia.featuredImageAlt || noticia.title,
    "alt"
  )} loading="lazy"> </div>`} <div class="noticia-content"> <div class="noticia-meta"> <time${addAttribute(noticia.date, "datetime")}> ${formatDate(noticia.date)} </time> ${noticia.categories.length > 0 && renderTemplate`<span class="category"> ${noticia.categories[0]} </span>`} </div> <h2 class="noticia-title"> ${noticia.title} </h2> <div class="noticia-excerpt">${unescapeHTML(noticia.excerpt)}</div> <a${addAttribute(noticia.link, "href")} class="read-more">
Leer más
<svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"> <path d="M5 12h14M12 5l7 7-7 7"></path> </svg> </a> </div> </article>`)} </div>` : renderTemplate`<p class="no-category-news">
No hay noticias de UWCL disponibles.
</p>`}` })) })}`;
}, "C:/Users/PC/madridfemeninoxtra/src/pages/noticias/index.astro", void 0);

const $$file = "C:/Users/PC/madridfemeninoxtra/src/pages/noticias/index.astro";
const $$url = "/noticias";

const _page = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
    __proto__: null,
    default: $$Index,
    file: $$file,
    url: $$url
}, Symbol.toStringTag, { value: 'Module' }));

const page = () => _page;

export { page };
